{
  "name": "dating-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web -- -p 3000",
    "dev:user": "npm run dev --workspace=web -- -p 3000",
    "dev:admin": "npm run dev --workspace=web -- -p 3001",
    "dev:agent": "npm run dev --workspace=web -- -p 3002",
    "build": "npm run build --workspace=web",
    "start": "npm run start --workspace=web",
    "lint": "npm run lint --workspace=web"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
